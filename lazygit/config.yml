gui:
  scrollHeight: 16
git:
  pagers:
    - colorArg: always
      pager: delta --dark --paging=never
customCommands:
  - key: "<c-r>"
    command: "git branch --set-upstream-to=origin/mainline && cr --publish --auto-merge"
    context: "global"
    output: log
